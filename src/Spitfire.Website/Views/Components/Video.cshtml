@model Spitfire.Library.Models.VideoModel

@if (Model.Item != null)
{
    <video controls muted="@Model.Mute" autoplay="@Model.Autoplay" loop="@Model.Loop" poster="" class="img img-responsive">
        <source src="@Model.VideoPath" type="@Model.VideoType">
    </video>
}
