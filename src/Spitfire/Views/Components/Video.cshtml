@model Spitfire.Models.VideoModel

@if (Model.Item != null)
{
    <video controls muted autoplay="" loop="" poster="" class="img img-responsive">
        <source src="@Model.VideoPath" type="@Model.VideoType">
    </video>
}
